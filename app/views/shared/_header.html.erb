<nav class="fixed top-0 left-0 right-0 z-50 px-6 md:px-12 py-4 bg-white/80 backdrop-blur-md shadow-sm">
  <div class="max-w-7xl mx-auto flex items-center justify-between">
    <div class="flex items-center gap-2">
      <a href="<%= root_path %>" class="text-2xl font-bold text-purple-400">Zignal</a>
    </div>

    <div class="hidden md:flex items-center gap-8">
      <a href="#features" class="text-gray-700 hover:text-purple-400 transition-colors">Features</a>
      <a href="#how-it-works" class="text-gray-700 hover:text-purple-400 transition-colors">How it works</a>
      <a href="#pricing" class="text-gray-700 hover:text-purple-400 transition-colors">Pricing</a>
    </div>

    <div class="flex items-center gap-4">
      <% if authenticated? %>
        <%= link_to "My Page", mypage_path, class: "text-white bg-purple-700 hover:bg-purple-800 focus:ring-4 focus:ring-purple-300 font-medium rounded-lg text-sm px-4 lg:px-5 py-2 lg:py-2.5 sm:mr-2" %>
        <%= button_to "Logout", session_path, method: :delete, class: "text-gray-800 hover:text-purple-700 font-medium rounded-lg text-sm px-4 lg:px-5 py-2 lg:py-2.5 lg:mr-0" %>
      <% else %>
        <%= link_to new_session_path do %>
          <button class="hidden md:inline-flex px-4 py-2 text-sm border-2 border-purple-300 text-purple-500 bg-transparent hover:bg-purple-50 rounded-xl font-medium transition-all duration-300">
            Log in
          </button>
        <% end %>
        <%= link_to new_registration_path do %>
          <button class="px-4 py-2 text-sm bg-purple-400 text-white hover:shadow-lg hover:shadow-purple-200/30 rounded-xl font-medium transition-all duration-300">
            Get Started
          </button>
        <% end %>
      <% end %>
    </div>
  </div>
</nav>
